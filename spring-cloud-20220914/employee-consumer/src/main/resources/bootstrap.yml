server:
  port: 9092
spring:
  application:
    name: employee-consumer
  cloud:
    config:
      uri: http://localhost:8092
management:
  endpoints:
    web:
      exposure:
        include: "*"
    health:
      show-details: always