server:
  port: 9091
spring:
  application:
    name: employee-producer
  cloud:
    config:
      uri: http://localhost:8092

management:
  endpoints:
    web:
      exposure:
        include: "*"
    health:
      show-details: always