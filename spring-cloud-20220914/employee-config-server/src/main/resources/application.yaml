spring:
  profiles:
    active: native
  application:
    name: employee-config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/common-config
server:
  port: 8092

